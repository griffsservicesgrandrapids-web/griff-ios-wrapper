import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.griff.gigconnect',
  appName: 'Gig Connect',
  webDir: 'www',
  bundledWebRuntime: false
};

export default config;
