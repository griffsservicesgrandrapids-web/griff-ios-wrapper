workflows:
  ios-build:
    name: Build iOS
    instance_type: mac_mini_m1
    max_build_duration: 120
    integrations:
      app_store_connect: APIKey

    environment:
      vars:
        XCODE_WORKSPACE: "ios/App/App.xcworkspace"
        XCODE_SCHEME: "App"

      node: 20
      xcode: latest
      cocoapods: default

    scripts:
      - name: Install npm dependencies
        script: |
          npm ci

      - name: Prepare iOS + Install CocoaPods
        script: |
          # Ensure iOS folder exists
          if [ ! -d "ios" ]; then
            npx cap add ios
          fi

          cd ios/App
          echo "Running pod install..."
          pod install --repo-update

          echo "=== Workspace check after pod install ==="
          ls -la App.xcworkspace || echo "❌ Workspace missing after pod install!"
          ls -la *.xcodeproj
          cd ../..

      - name: Build web assets and sync Capacitor
        script: |
          npm run build
          ls -la dist/ || echo "ERROR: dist/ missing!"

          npx cap sync ios --no-open

          echo "=== Contents of ios/App after cap sync ==="
          ls -la ios/App

      - name: Set up code signing
        script: |
          xcode-project use-profiles

      - name: Build IPA
        script: |
          xcode-project build-ipa \
            --workspace "$XCODE_WORKSPACE" \
            --scheme "$XCODE_SCHEME" \
            --archive-flags="-destination 'generic/platform=iOS'"

    artifacts:
      - build/ios/ipa/*.ipa
      - /tmp/xcodebuild_logs/*.log

    publishing:
      app_store_connect:
        auth: integration
        submit_to_testflight: true
