workflows:
  ios-release:
    name: Gig Connect iOS Build
    max_build_duration: 60
    integrations:
      app_store_connect: APIKey
    environment:
      ios_signing:
        distribution_type: app_store
        bundle_identifier: com.griff.gigconnect
    scripts:
      - npm ci
      - npm install @capacitor/cli@latest @capacitor/core@latest @capacitor/ios@latest
      - npm run build
      - mkdir -p www && cp -r dist/* www/ || true
      - ls -la www
      - rm -rf ios || true
      - npx cap add ios
      - npx cap sync ios
      - ls -la ios/App  # debug: should show App.xcodeproj
      - cd ios/App
      - |
        if [ ! -f Podfile ]; then
          echo "Manual Podfile creation with absolute path"
          echo "platform :ios, 13.0" > Podfile
          echo "use_frameworks!" >> Podfile
          echo "" >> Podfile
          echo "project './App.xcodeproj'" >> Podfile  # explicit current dir
          echo "" >> Podfile
          echo "target 'App' do" >> Podfile
          echo "  pod 'Capacitor', :path => '../../node_modules/@capacitor/ios'" >> Podfile
          echo "  pod 'CapacitorCordova', :path => '../../node_modules/@capacitor/ios/CapacitorCordova'" >> Podfile
          echo "end" >> Podfile
          echo "" >> Podfile
          echo "post_install do |installer|" >> Podfile
          echo "  installer.pods_project.targets.each do |target|" >> Podfile
          echo "    target.build_configurations.each do |config|" >> Podfile
          echo "      config.build_settings['IPHONEOS_DEPLOYMENT_TARGET'] = '13.0'" >> Podfile
          echo "    end" >> Podfile
          echo "  end" >> Podfile
          echo "end" >> Podfile
        fi
      - ls -la  # debug: confirm Podfile and App.xcodeproj are here
      - pod repo update || true
      - pod install --repo-update
      - cd ../../
      - xcode-project use-profiles
      - cd ios/App
      - xcodebuild -workspace App.xcworkspace -scheme App -configuration Release -sdk iphoneos -derivedDataPath build -archivePath build/App.xcarchive archive
    artifacts:
      - ios/App/build/ios/ipa/*.ipa