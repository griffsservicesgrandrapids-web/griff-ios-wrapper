workflows:
  ios-build:
    name: Build iOS
    instance_type: mac_mini_m1
    max_build_duration: 120
    integrations:
      app_store_connect: APIKey   # your existing integration

    environment:
      ios_signing:
        distribution_type: app_store      # Change to "ad_hoc" only if you need Ad Hoc testing
        bundle_identifier: com.yourcompany.yourapp   # ← MUST match your app's real bundle ID exactly

      vars:
        XCODE_WORKSPACE: "ios/App/App.xcworkspace"
        XCODE_SCHEME: "App"

      node: 20
      xcode: latest                     # or pin to a stable version if Xcode 26.2 causes issues
      cocoapods: default

    scripts:
      - name: Install npm dependencies
        script: |
          npm ci

      - name: Build web assets
        script: |
          npm run build
          ls -la dist/ || echo "ERROR: dist/ missing after build!"

      - name: Sync Capacitor iOS + CocoaPods
        script: |
          # Clean previous iOS folder only if needed (avoid aggressive rm every time)
          npx cap sync ios --no-open

          cd ios/App
          pod install --repo-update
          cd ../..

          # Verify workspace exists
          ls -la "$XCODE_WORKSPACE" || echo "WARNING: Workspace still missing!"

      - name: Set up code signing
        script: |
          xcode-project use-profiles

      - name: Build IPA
        script: |
          xcode-project build-ipa \
            --workspace "$XCODE_WORKSPACE" \
            --scheme "$XCODE_SCHEME" \
            --archive-flags="-destination 'generic/platform=iOS'"

    artifacts:
      - build/ios/ipa/*.ipa
      - /tmp/xcodebuild_logs/*.log

    publishing:
      app_store_connect:
        auth: integration
        submit_to_testflight: true
